"""
Auth package — v3.2.1 minimal administrative authentication (foundation only).

This module defines the backend auth area boundaries. Phase 1 (v3.2.1) only
sets up structure and contracts; concrete login, token, and protection
behavior are implemented in later phases.
"""


# Gemini API Configuration
GEMINI_API_KEY=your_api_key_here

# Frame Extraction Settings
EXTRACT_FPS=1.0
# MAX_FRAMES_TO_SEND: vacío o 0 = sin límite (procesar todo el video). Solo definir para debug/performance.
# MAX_FRAMES_TO_SEND=200
FRAME_STRIDE=1
# TIME_LIMIT_SEC= opcional, procesar solo frames con timestamp <= N segundos

# Image Preprocessing
RESIZE_MAX_SIDE=1280

# Output Configuration
OUTPUT_DIR=output
DEBUG_SAVE_FRAMES=false

# Model Configuration (optional)
GEMINI_MODEL_NAME=gemini-2.0-flash-exp

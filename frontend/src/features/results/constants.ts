/**
 * Epic 3 — Feature-level constants for the Results overview.
 * Thresholds live in src/constants/reviewThresholds.ts (shared with status alignment).
 */

export { LOW_CONFIDENCE_THRESHOLD } from '../../constants/reviewThresholds';

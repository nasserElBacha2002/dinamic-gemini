/**
 * Sprint 5.3 — shared i18n keys for operator-facing copy where feature code imports constants.
 * Use `t(key)` with these keys from translation.json (default namespace).
 */

export const INVENTORY_LIST_EMPTY_TITLE_KEY = 'inventory.list_empty_title' as const;
export const INVENTORY_LIST_EMPTY_MESSAGE_KEY = 'inventory.list_empty_message' as const;

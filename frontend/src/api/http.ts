/**
 * F5.1.1 scaffold.
 *
 * Shared HTTP helpers (API_BASE, protectedFetch, response/error parsing)
 * stay in client.ts for now to keep this phase low-risk.
 * They will be extracted in a later sub-phase.
 */
export {};

/**
 * Layout widths — aligned with shell (Sprint 2.2) and theme direction (Re diseño 3.3 §7.2 desktop-first).
 * Keep in sync with product expectations for operational tables vs. narrow review columns.
 */
export const MAIN_CONTENT_MAX_WIDTH_PX = 1400;

/**
 * Max width for focused review/detail columns inside `AppMain` (Re diseño 3.3 §9.9).
 */
export const DETAIL_COLUMN_MAX_WIDTH_PX = 700;

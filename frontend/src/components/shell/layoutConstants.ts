/**
 * Default max width for focused review/detail columns inside `AppMain` (Re diseño 3.3 §9.9 two-column intent).
 * List and dashboard-style pages use the full `AppMain` width; narrow strips apply at page level.
 */
export const DETAIL_COLUMN_MAX_WIDTH_PX = 700;

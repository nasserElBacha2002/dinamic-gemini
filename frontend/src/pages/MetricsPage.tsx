import PlaceholderScreenScaffold from '../components/shell/PlaceholderScreenScaffold';

/** Sprint 2.1 scaffold — Re diseño 3.3 §9.11 (analytics separated from daily operations). */
export default function MetricsPage() {
  return (
    <PlaceholderScreenScaffold
      roadmapNote="Trends, correction rates, and performance tables will use metrics aggregates (Plan 3.3 — Fase 5)."
    />
  );
}

import PlaceholderScreenScaffold from '../components/shell/PlaceholderScreenScaffold';

/** Sprint 2.1 scaffold — Re diseño 3.3 §9.2 (KPI row, attention, activity, recent inventories). */
export default function DashboardPage() {
  return (
    <PlaceholderScreenScaffold
      roadmapNote="Operational KPIs, Requires attention, Recent activity, and Recent inventories table will connect to dashboard summary contracts (Plan 3.3 — Fase 1 / Sprint 1.5)."
    />
  );
}

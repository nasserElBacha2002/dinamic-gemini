/** Default on-device transform parameters for `photoPrepare`. Overridable per call. */
export const DEFAULT_MAX_DIMENSION_PX = 3000;
export const DEFAULT_JPEG_COMPRESSION_RESIZE = 0.85;
export const DEFAULT_JPEG_COMPRESSION_CONVERT_ONLY = 0.92;

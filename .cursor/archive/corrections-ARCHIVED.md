# corrections (archived)

This command was tied to a one-off fix list (bbox parsing, dedupe, evidence_index, jobs result mode, config). It has been replaced by **corrections-scoped** for reusable, scope-controlled corrective refactors.

See `.cursor/commands/corrections-scoped.md` for the current command.
